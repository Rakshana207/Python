# to find the factorial of given number
def factors(n):
    a=[]
    for i in range(1,n+1):
        if(n%i==0):
            a.append(i)
    return a
num=int(input("enter the number:"))
print("the factors of given number :",factors(num))
